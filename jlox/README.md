# jlox

The Java tree-walk interpreter implementation.

At this stage, this directory only contains the minimal entry skeleton. The next steps follow the book in order:

- scanner
- parser
- AST generation
- interpreter
- resolver

Build and run entry points stay at the repository root through `make jlox` and `make run-jlox`.

